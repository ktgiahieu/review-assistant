# Replace Sections

To replace sections in one HTML from another HTML use `run_replace_sections.py`

To run the code:
```bash
python3 run_replace_sections.py
```

Note: Make sure to configure input folder (with HTML1 and HTML2), and output file names in `run_replace_sections.py`
